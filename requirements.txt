# Dependencias do Defensor IA
# Versoes fixas para builds reprodutiveis e mitigacao de supply-chain risk (M3).
# Para atualizar: pip install -U <pacote>, testar, e ajustar a versao aqui.
# Gerar lockfile completo com: pip freeze > requirements.lock

streamlit==1.57.0
voyageai==0.3.7
groq==1.2.0
supabase==2.30.0
pymupdf==1.27.2.3
python-dotenv==1.2.2
requests==2.32.5    # P2.8 - checagem HIBP de senha vazada
